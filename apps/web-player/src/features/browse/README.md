# browse

| File | Purpose |
|------|---------|
| `BrowsePage.tsx` | Movies/Series page: category chips + `PagedGrid` (100 per page, "Load more"). |
| `SearchPage.tsx` | Debounced search across movies and series. |
