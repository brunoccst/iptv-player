# profiles

| File                | Purpose                                                                                                   |
| ------------------- | --------------------------------------------------------------------------------------------------------- |
| `ProfilePicker.tsx` | "Who's watching?" grid, add tile (max 5), manage mode, profile editor modal (name, colour, kids, delete). |
| `avatar.ts`         | Avatar colour palette; `avatarKey` stores the chosen colour.                                              |
