# src

| File | Purpose |
|------|---------|
| `index.ts` | Native bindings: `TvMedia` module, `TvPlayerView` component. |
| `types-only.ts` | Event, prop and download types (no native calls; reused by the Jest mock). |
