# modules

Local Expo native modules, auto-linked by `expo prebuild`.

| Module | Purpose |
|--------|---------|
| [`tv-media`](./tv-media) | ExoPlayer view + Media3 offline downloads in app-private storage. |
