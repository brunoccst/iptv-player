# title_normalizer

Pure Python package. No Azure SDK imports, so it runs and tests without the Functions host.

| Module | Purpose |
|--------|---------|
| `config.py` | Loads `Settings` from root `.env` / environment. |
