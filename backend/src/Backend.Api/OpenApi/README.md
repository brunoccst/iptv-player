# OpenApi

`RequiredPropertiesSchemaTransformer.cs`: marks all object properties `required` in the OpenAPI document (nullable ones stay nullable). The build writes the document to `packages/shared/openapi/backend-openapi.json`.
