# Errors

`Problems.cs`: validation problem helper and `ProducesProviderErrors()` OpenAPI metadata.

`ProviderExceptionHandler.cs`: turns `ProviderException` into `502` problem details. `ErrorCodes` lists every `code` value clients can receive.
