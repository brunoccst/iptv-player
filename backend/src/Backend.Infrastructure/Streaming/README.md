# Streaming

| File | Purpose |
|------|---------|
| `PlaybackService.cs` | Builds the client-facing playback URL (relay or direct). |
| `RelayTokenService.cs` | Encrypts + signs upstream URLs into expiring relay tokens and paths. |
