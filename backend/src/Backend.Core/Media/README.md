# Media

`MediaModels.cs`: records returned by every provider and serialized to clients (`MediaCategory`, `LiveChannel`, `MovieSummary`, `MovieDetails`, `SeriesSummary`, `SeriesDetails`, `Season`, `Episode`).
