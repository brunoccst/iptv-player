# Library

`PipelineEntities.cs`: rows of `pipeline.db` — `NormalizationJob`, `MasterMedia`, `MediaVariant` — plus `JobStatus`, `LibraryKind` constants and the `LibraryPayloadItem` job payload shape. Timestamps are Unix seconds.
